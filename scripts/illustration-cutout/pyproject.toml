[project]
name = "illustration-cutout"
version = "0"
description = "Cut out and size-normalize Midjourney kanji illustrations into transparent PNG assets. Dev-only, never imported by the app."
requires-python = ">=3.12,<3.13"
dependencies = [
    "rembg>=2.0.59,<3",
    "onnxruntime>=1.19,<2",
    "pillow>=10.4,<12",
    "numpy>=1.26,<3",
]

[dependency-groups]
dev = ["pytest>=8,<9"]

[tool.uv]
package = false
